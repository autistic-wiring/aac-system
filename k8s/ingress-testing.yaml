apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aac-board-testing-ingress
  namespace: asd
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: aac-testing.nexvision.cc
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: aac-board-testing
                port:
                  number: 80
  tls:
    - hosts:
        - aac-testing.nexvision.cc
      secretName: aac-board-testing-tls
