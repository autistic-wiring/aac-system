---
title: "AAC Speech Pipeline"
date_modified: 2026-07-16
tags: [speech, tts, audio, sherpa-onnx, offline]
---

# Speech Pipeline

Defined in `src/utils/speechAdapter.js` and `tts-server.js`.

## 3-Layer Fallback Chain

1. **Pre-generated WAV files** (`public/audio/`) — `AudioBuffer` cache via Web Audio API. Generated by `scripts/generate-audio.js` using Sherpa-ONNX with Piper VITS `en_US-lessac-medium` at 22,050 Hz, speed 0.55.
2. **TTS HTTP server** (`tts-server.js`) — Node.js server on port 5050, `GET /speak?text=...` returns WAV. Same Piper model, pitch-scaled +50 cents, gain 1.15, speed 0.55.
3. **Browser SpeechSynthesis API** — rate 0.72, pitch 1.1.

## Audio Processing Scripts

- `scripts/pad-audio.mjs` — prepends 150ms silence to prevent mobile audio hardware warmup clipping
- `scripts/fix-onset.mjs` — boosts quiet onset samples (fricatives/nasals) for mobile hardware

## PWA Caching

TTS server at `http://127.0.0.1:5050/` uses `NetworkOnly` Workbox strategy — no caching.

## TTS Model

`tts-models/vits-piper-en_US-lessac-medium/` — ONNX model + espeak-ng data + tokens. Speed 0.55 for child-friendly pacing.

## Custom Pronunciation

Vocabulary items can have a `pronounce` field to override TTS text for words the engine mispronounces.

See [[vocabulary-system]] for the data model.
